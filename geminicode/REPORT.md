# QA 差异反查 SAE 神经元分析报告 (geminicode)

## 实验概况
- **目标模型**: Gemma-4-E2B-it
- **数据集**: `gemma_code_GB01.json` (TargetedDrugs 专题)
- **分析方法**: 对比 Case A (失败，LeetCode 未通过) 与 Case B (教师模型修正，通过) 的 SAE 神经元激活分布。
- **阶段划分**: 
    - **THINK**: 模型生成 `cot` (Thinking Process) 的阶段。
    - **OUTPUT**: 模型生成 `response` (最终 C++ 代码) 的阶段。

## 核心结果总结

### 1. THINK 阶段 (逻辑规划)
在思考阶段，Case B (高质量) 相比 Case A (低质量) 显示出明显的“专注”特征：
- **正向强化 (B > A)**:
    - `L00#3567` (diff=+2.397): 极早期神经元，在正确逻辑链中被强烈唤醒。
    - `L04#825` (diff=+0.952, rate_B=0.632): 中层逻辑对齐神经元。
- **负向抑制 (B < A)**:
    - `L01#4433` (diff=-1.834, rate_B 从 0.106 跳至 0.521? *注: 这里 diff 是 B-A，负数代表 A > B*): 
        - 修正：`L01#4433` 在 A 中激活更弱，在 B 中更强（diff 是 B-A）。
        - 关键：`L01#5306` (diff=-1.447) 保持 1.0 激活率但均值下降，说明是基调调整。

### 2. OUTPUT 阶段 (代码生成)
在生成阶段，差异主要集中在早期层（输入处理）和极末层（语义决策）：
- **失败特征 (A > B)**:
    - `L00#4554` (diff=+2.968, *注: diff=B-A，此处代表 A 显著强于 B*): 这是一个典型的“错误路径指示器”。当模型进入 Case A 的错误逻辑时，该神经元异常活跃。
- **成功特征 (B > A)**:
    - `L04#4543` (diff=+2.491): 早期代码结构锚点。
    - `L33#9054` (diff=+1.966, *注: B 均值高于 A*): 深层语义对齐。

## 结论与建议
1. **L00#4554** 是一个非常强的“错误信号”，在后续的 Steering 实验中，可以尝试通过负向 Clamp (-2.0) 来抑制该神经元，看是否能让模型避开 Case A 的错误模式。
2. **L00#3567** 是“成功逻辑”的潜在进入点。
3. **协作性提示**: 原始统计数据已保存至 `outputs/diff_stats.pt`，可使用 `torch.load` 加载进行更细致的区间分析。

---
Generated by Gemini Code Helper.
Date: 2026-04-28
