这是一个经典的数位DP（Digit DP）问题，或者可以通过数学递推的方式来解决。由于 $n$ 的范围是 $0 \le n \le 10^9$，我们可以采用更直接的数学方法来计算。

### 方法一：数学递推（基于位数）

我们可以计算在 $[0, n]$ 范围内，每一位（个位、十位、百位等）上数字 $1$ 出现的总次数。

考虑一个数字 $n$。我们从最高位开始，逐位计算。

假设 $n$ 的数字表示是 $d_k d_{k-1} \dots d_1 d_0$。

**核心思想：** 统计在所有小于 $n$ 的数字中，以及在 $n$ 本身中，数字 $1$ 的出现次数。

我们使用一个函数来计算在 $[0, n]$ 范围内，某个特定位置（比如 $10^i$ 的位）上数字 $1$ 出现的总次数。

**更简洁的数学方法（基于位）：**

我们计算在 $[0, n]$ 范围内，对于每一位 $i$（$10^i$ 的位），数字 $1$ 出现的总次数。

设 $n$ 的字符串表示为 $S$。长度为 $L$。

1. **处理所有长度小于 $L$ 的数字：**
   对于长度为 $l < L$ 的所有数字，在任何一位上，数字 $1$ 的出现次数是固定的。
   * 对于 $l$ 位数，总共有 $l \times 10^{l-1}$ 个数字。
   * 在任何一位上，数字 $1$ 出现的次数是 $10^{l-1}$ 次（例如，在个位上，1, 11, 21, ..., 91，共 10 个，但我们关注的是 $1$ 出现的次数）。
   * 更简单地看：在 $l$ 位数中，总共有 $l \times 10^{l-1}$ 个数字。在任何一位上，数字 $1$ 出现的次数是 $10^{l-1}$ 次。
   * **更准确的计数：** 对于长度 $l$，在任何一位上，数字 $1$ 出现的次数是 $10^{l-1}$ 次。
   * 我们可以直接计算所有长度小于 $L$ 的数字中 $1$ 的总数。

2. **处理长度等于 $L$ 的数字（使用 $n$ 的结构）：**
   我们从最高位 $i=L-1$ 开始，逐位比较 $n$ 的数字 $d_i$。

   * **对于当前位 $i$：**
     * **如果 $d_i > 1$：** 那么所有小于 $d_i$ 的数字（即 $0$ 到 $d_i - 1$）在当前位上都可以是 $1$。
       * 贡献：$1 \times 10^i$ (因为后面 $i$ 位可以任意取值)。
     * **如果 $d_i = 1$：** 那么当前位是 $1$ 的情况，贡献 $1$。
     * **如果 $d_i < 1$ (即 $d_i = 0$)：** 那么当前位不能是 $1$（因为我们只考虑非负整数，且当前位不能是 $1$ 导致数字大于 $n$）。

   * **处理 $n$ 本身：** 检查 $n$ 中是否包含 $1$。

### 最终采用的实现思路（基于位 DP 思想的简化计数）

我们使用一个更标准的位 DP 计数方法，但由于 $N$ 最大是 $10^9$，直接用字符串处理是最清晰的。

设 $S$ 是 $n$ 的字符串表示。

**步骤 1：计算所有长度小于 $L$ 的数字中的 $1$ 的数量。**
对于长度 $l=1$ 到 $L-1$：
* 长度为 $l$ 的数字有 $9 \times 10^{l-1}$ 个（如果 $l>1$），或者 $10$ 个（如果 $l=1$）。
* 在 $l$ 位数中，总共有 $l \times 10^{l-1}$ 个数字的位。
* 在任何一位上，数字 $1$ 出现的次数是 $10^{l-1}$ 次。
* 总贡献：$\sum_{l=1}^{L-1} l \times 10^{l-1}$ (这个公式是错误的，需要更精确的计数)。

**更精确的计数（基于 $10^k$ 的位）：**

我们计算 $n$ 的每一位 $i$（从 $0$ 开始，对应 $10^i$ 的位）。

1. **固定 $i$ (当前位)，计算 $1$ 的贡献：**
   * 设 $P = 10^i$ (当前位权)， $Q = 10^{i+1}$ (下一位权)。
   * $A = n / Q$ (高位部分)
   * $B = n \% P$ (低位部分)
   * $D = (n / P) \% 10$ (当前位数字 $d_i$)

   * **贡献 1：** 考虑所有高位 $A' < A$ 的情况。对于每一个 $A'$, 当前位可以取 $1$。贡献 $A' \times P$。
   * **贡献 2：** 考虑高位等于 $A$ 的情况。
     * 如果 $D > 1$，当前位可以取 $1$。贡献 $P$。
     * 如果 $D = 1$，当前位可以取 $0$。贡献 $0$ (因为 $1$ 已经包含在 $A$ 的计算中，或者我们处理 $A$ 的情况)。

这个方法非常复杂且容易出错。**我们采用最标准的 Digit DP 结构，但用迭代实现。**

---

### 最终 C++ 代码实现 (基于迭代/数学方法)

我们使用一个辅助函数来计算在 $[0, n]$ 范围内，特定位上 $1$ 的数量。

```cpp
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

class Solution {
private:
    /**
     * 辅助函数：计算在 [0, n] 范围内，特定位 (power_of_10) 上数字 1 出现的总次数。
     * 
     * @param n 目标上限
     * @param power_of_10 当前位权 (1, 10, 100, ...)
     * @return 在所有数字中，该位上数字 1 的总出现次数。
     */
    long long countOnesAtPosition(int n, long long power_of_10) {
        if (power_of_10 == 0) return 0; // 避免除以0或无限循环

        long long count = 0;
        long long p = power_of_10;
        long long q = p * 10;

        // A: n 的高位部分 (n / (p * 10))
        // B: n 的当前位 (n % p)
        // D: n 的当前位数字 ( (n / p) % 10 )
        long long A = n / q;
        long long B = n % p;
        int D = (n / p) % 10;

        // 1. 考虑所有高位 A' < A 的情况
        // 对于每一个 A' (从 0 到 A-1)，当前位可以取 1。
        // 贡献 = A * p (因为 A 包含了 0 到 A-1 的所有前缀)
        count += A * p;

        // 2. 考虑高位等于 A 的情况
        if (D > 1) {
            // 如果当前位 D > 1，那么当前位可以取 1，且数字仍然 <= n。
            count += p;
        } else if (D == 1) {
            // 如果当前位 D == 1，那么当前位只能取 0 或 1。
            // 只有当当前位取 1 时，数字才可能等于 n 的前缀。
            // 此时，我们只计算到 n 的当前位为止的贡献，即 1 次。
            count += 1;
        }
        // 如果 D == 0，则当前位不能取 1 (否则数字会大于 n)，贡献为 0。

        return count;
    }

public:
    int countDigitOne(int n) {
        if (n < 0) return 0;
        if (n == 0) return 0;

        long long total_ones = 0;
        long long power_of_10 = 1;

        // 循环处理从个位 (10^0) 开始的所有位
        while (power_of_10 <= n) {
            total_ones += countOnesAtPosition(n, power_of_10);
            
            // 检查是否会溢出，虽然 n <= 10^9，long long 足够
            if (power_of_10 > 1000000000LL) break; 
            power_of_10 *= 10;
        }

        // 由于 n <= 10^9，结果不会超过 10^10，int 足够存储最终结果
        return (int)total_ones;
    }
};
```<turn|>